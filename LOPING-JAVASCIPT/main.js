// for, fo of, fo in, foreach
// while & do while

const paca = ["bunnga", "una", "roberrt"];
console.log(paca);

for (let pacar in paca) {
  console.log(pacar);
}

// paca.forEach((element, index) => {
//   console.log(element, index + 2);
// });

paca.forEach((element) => {
  console.log(element);
});
